{
	"name": "resilient-mailer-ses",
	"version": "0.1.2",
	"description": "AWS SES provider implementation for resilient-mailer",

	"license": "MIT",
	"author": "James Billingham <james@jamesbillingham.com> (http://jamesbillingham.com)",

	"main": "lib/index.js",

	"repository": {
		"type": "git",
		"url": "https://github.com/cuvva/resilient-mailer-ses.git"
	},

	"scripts": {
		"test": "tape test/*.js",
		"test-travis": "istanbul cover test/*.js --report lcovonly"
	},

	"dependencies": {
		"aws-sdk": "^2.1.29"
	},

	"devDependencies": {
		"tape": "^4.0.0",
		"istanbul": "^0.3.14"
	}
}
